# MongoDB connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/ecosmart

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_key_here

# Port (optional, defaults to 3000)
PORT=3000
